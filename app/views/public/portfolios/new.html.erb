<div class="container">
  <div class="row">
    <div class="col">
      <h4 class="my-3">投稿フォーム</h4>
      <%= render 'form', portfolio: @portfolio %>
    </div>
  </div>
</div>

<!--render後のURLを指定-->
<%= render 'layouts/specify_url', url: "/portfolios/new" %>